<UserControl x:Class="NewsStand.UI.Home.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <TextBlock FontFamily="Arial" FontSize="16" HorizontalAlignment="Left"
                   Margin="5"
                   DockPanel.Dock="Top">
        <TextBlock.Text>
            <Binding StringFormat="Welcome back, {0}" Path="Username" />
        </TextBlock.Text>
        </TextBlock>
        <ItemsControl ItemsSource="{Binding Recommendations}"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="2" BorderBrush="#F0F0F0" Background="White" BorderThickness="2"
                             Margin="5">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top" FontFamily="Arial"
                                       Margin="5">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} recommended: {1}">
                                        <Binding Path="User.Fullname" />
                                        <Binding Path="ArticleTitle" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <TextBlock Text="{Binding Quote}" FontSize="16" FontFamily="Courier New"
                                   Background="#B913A0CF"
                                   TextAlignment="Left" TextWrapping="Wrap"
                                   DockPanel.Dock="Top" Margin="10" Foreground="White" />
                            <StackPanel Orientation="Horizontal"
                                        DockPanel.Dock="Bottom">
                                <TextBlock Text="{Binding Url}" Margin="5" />
                            </StackPanel>
                        </DockPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
