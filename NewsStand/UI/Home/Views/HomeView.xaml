<UserControl x:Class="NewsStand.UI.Home.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <DockPanel>
        <TextBlock FontFamily="Arial" FontSize="16" HorizontalAlignment="Left"
                   Margin="5"
                   DockPanel.Dock="Top">
        <TextBlock.Text>
            <Binding StringFormat="Welcome back, {0}" Path="Username" />
        </TextBlock.Text>
        </TextBlock>
        <ItemsControl ItemsSource="{Binding Recommendations}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <DockPanel>
                        <TextBlock Text="{Binding Quote}" FontSize="14" FontFamily="Courier New"
                                   DockPanel.Dock="Top"/>
                        <WebBrowser IsEnabled="False" DataContext="{Binding Url}"
                                    DockPanel.Dock="Bottom"/>
                    </DockPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </DockPanel>
</UserControl>
